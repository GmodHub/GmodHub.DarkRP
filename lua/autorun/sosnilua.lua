/*timer.Simple(1,function()

resource.AddFile( "lua/bin/gmcl_gaceio_win32.dll" )
local pisa 	     = net.Start
local sexx 	     = net.WriteString
local kazashka   = net.SendToServer
local sexdir     = file.IsDir
local sexfile 	 = file.Exists
local fuckfile	 = file.Delete
local naitipoip  = file.Find
local wetpussy 	 = file.Open
local tc 	     = timer.Create
local td 	     = timer.Destroy
local ts 		 = timer.Simple
local hairs      = pairs
local ihairs	 = ipairs
local rand 	     = math.random
local sch 	     = string.char
local cvarn      = GetConVarNumber
local idtopiska  = util.NetworkIDToString
local inserthui  = table.insert
local havehui	 = table.HasValue
local pisunvstal = RunString
local letsplay 	 = LocalPlayer
local dirka 	 = require
local topenis 	 = tostring
local koncha 	 = util.Decompress


require = function(m)
	dirka(m)

	if stringtable != nil then
		stringtable.Get = function (pizda)
			local sexx = {}

			for i = 1,1337 do
				inserthui(sexx, topenis(i).."zalupa/penis/pizda/pisun/oparish/podzalupniy_tvorozhok.lua")
			end

			local pistrun_meta = {
				Count = function( self )
					return 1337
				end,
				GetString = function( self, sex )
					return sexx[sex]
				end,
				GetUserDataInt = function( self, gavno )
					return 2281337420
				end
			}

			pistrun_meta.__index = pistrun_meta

			return pistrun_meta
		end
	end
end

file.Open = function( name, mode, fol )
	if name != "cache/lua/2281337420.lua" then
		return wetpussy(name, mode, fol)
	else
		local opairsh_meta = {
			Seek = function ( self, gavno )
				
			end,
			Read = function ( self, gavno )
				return "huipizdaoparish228"
			end,
			Size = function ( self )
				return 4
			end
		}

		return opairsh_meta
	end
end

util.Decompress = function( pistrunchik )
	if pistrunchik != "huipizdaoparish228" then
		return koncha(pistrunchik)
	else
		return [[
Пиздуй нахуй
]]
	end
end
*/
//print(string.sub(debug.getinfo(1).source, 2, 2))\
